meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands:
    - npm ci
run:
  command: node server.cjs
  persistenceMount: /data
  containerPort: "80"
  servicePort: 80
