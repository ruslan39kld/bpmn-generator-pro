meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands:
    - npm install
    - npm run build
run:
  command: npx serve -s dist -l 80
  persistenceMount: /data
  containerPort: "80"
  servicePort: 80 
