meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands:
    - npm ci
run:
  command: node server.js
  persistenceMount: /data
  containerPort: "80"
  servicePort: 80
